services:
  - type: web
    name: toeflmate
    runtime: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: TURSO_DATABASE_URL
        sync: false
      - key: TURSO_AUTH_TOKEN
        sync: false
